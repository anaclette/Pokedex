import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: 'black',
    flex: 1,
    opacity: 0.5,
  },
  pokeballIcon: {
    position: 'absolute',
    alignSelf: 'center',
    top: '60%',
  },
});
