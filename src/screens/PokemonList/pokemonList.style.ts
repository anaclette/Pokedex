import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  loaderHeight: {
    alignSelf: 'center',
    height: 400,
  },
  poster: {
    width: '100%',
    position: 'absolute',
    resizeMode: 'cover',
  },
});
