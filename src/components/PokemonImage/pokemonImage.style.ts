import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  imageContainer: {
    alignSelf: 'center',
    position: 'absolute',
    zIndex: 2,
    right: '10%',
  },
  image: {
    flex: 1,
  },
});
